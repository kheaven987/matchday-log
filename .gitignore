# Installers — unrelated to the app, not meant to be published
*.exe

# Personal data exports — real club/venue/fixture data, not for the public repo
matchday-log-backup-*.json

# Superseded local snapshots — matchday-log (2).html was renamed to index.html,
# which git now versions going forward. Kept on disk for reference, not published.
matchday-log.html
matchday-log (1).html

# Wrangler (Cloudflare Workers CLI) local state — never contains the secret itself
# (that's stored in Cloudflare, not here), but no reason to track it either
.wrangler/
node_modules/
